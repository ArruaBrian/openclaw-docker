services:
  openclaw:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "18789:18789"
    volumes:
      # Persistent data — survives rebuilds and redeploys
      - openclaw-data:/home/node/.openclaw
      - openclaw-workspace:/home/node/workspace
      # Exec approvals config (bind mount so you can edit without rebuild)
      - ./config/exec-approvals.json:/home/node/.openclaw/exec-approvals.json
    environment:
      - OPENCLAW_GATEWAY_TOKEN=${OPENCLAW_GATEWAY_TOKEN}
      - DISCORD_BOT_TOKEN=${DISCORD_BOT_TOKEN}
      - MINIMAX_API_KEY=${MINIMAX_API_KEY}
      - OPENCLAW_HOME=/home/node
    shm_size: 2g

volumes:
  openclaw-data:
  openclaw-workspace:
